name: external-libraries
requiredPermissions:
  - fetch
functions:
  downloadExternalLibraries:
    path: plug/external-libraries.ts:downloadExternalLibraries
    command:
      name: "External Libraries: Update"
