name: github-libraries
requiredPermissions:
  - fetch
functions:
  downloadExternalLibraries:
    path: github-libraries.ts:downloadExternalLibraries
    command:
      name: "Download External Libraries"
