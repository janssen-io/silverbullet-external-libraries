name: github-libraries
requiredPermissions:
  - fetch
functions:
  downloadExternalLibraries:
    path: plug/github-libraries.ts:downloadExternalLibraries
    command:
      name: "External Libraries: Update"
